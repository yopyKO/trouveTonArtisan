import { SpecialtyFilterPipe } from './specialty-filter.pipe';

describe('SpecialtyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialtyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
