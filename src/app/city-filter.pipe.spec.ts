import { CityFilterPipe } from './city-filter.pipe';

describe('CityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
